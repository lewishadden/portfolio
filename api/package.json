{
  "name": "portfolio-backend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun ./src/server.js",
    "build": "bun build ./src/server.js --target=bun --minify --outfile=index.js",
    "build:image": "docker buildx build --pull -t lewishadden/portfolio-nodemailer ."
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "nodemailer": "^6.9.9",
    "path": "^0.12.7"
  }
}
